Change this into what i need to send you

typedef_struct s_cmd_var
{
	char	**cmd;
		// cmd[0] = cmd
		// cmd[ect] = flags ect

	int			fd[2];
	t_cmd_var	*next;
}			t_cmd_var;

typedef struct s_cmds
{
	t_cmds	*first;
}			t_cmds;