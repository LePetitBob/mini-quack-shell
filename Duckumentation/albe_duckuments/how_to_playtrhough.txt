0_parsing av
{
	get ac av env
	check files given after "<" ">" ">>"
		open files depending on "<" ">" ">>"
	separate each cmd into a linked list (each cmd has a struct)
	use access on all the cmds found
}
1_resolution
{
	if no space in "'$ -> fuse
	for each ' "
		find the next one
	for $(PATH)
		get path in env (just like cmd)
}
1_here_doc
{
	for each "<<"
		add "limiter" to (char **)
		read from stdin
		each time you find limiter[i]
			++i
		if i = max
			return (char **) with everything from stdin
	for each "<<" pipe(pipehd[2])
	put 
}
2_redirection
{
	"<" && ">" ">>"
	for each "<" open file with "O_RDONLY"
		add fd[0] = file
	for each ">" open file with "O_WRONLY | O_TRUNC"
		add fd[1] = file
	for each ">>" open files with "O_WRONLY | O_APPEND"
		add fd[1] = file
}
3_send_Vincent
{
	send pointer to first element of the list
}
