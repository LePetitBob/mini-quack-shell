Vincent :

cat srcs/debug/m_alltest | valgrind  ./minishell &>srcs/debug/m_log.txt

test tricky redir when implemented (echo lol >/dev/full)
cd -> CDPATH

ON PEUT PLUS LQNCER MINISHELL DANS MINISHELL
Check la valeur d

------------------------

Albe :

ENV VAR IN HERE_DOC MUST BE EXPANDED

echo "h ey" > hey| echo hey>hey
- error fd -> check parsing
- make syntax error for ';'
- make syntax error for '&'
- make syntax error for '&&'

make "ambigous redirect" error for env var not defined

----------------------------

<< a cat | << b cat | cat << c
<< a a | << b a | a << c

export TMPPATH=$PATH
unset PATH
echo $PATH
export PATH=$TMPPATH
unset TMPPATH
echo $PATH

----------------------------------------------------

"h ey" hey|<hey

echo $$$$$$
	-> not a bug, can't get pid without "getpid" (forbidden)

echo gerigh >/dev/stdout| ls -las

echo bonjour > $test
export test="o1 o2"
echo bonjour > $test

mkdir a
mkdir a/b
cd a/b
rm -r ../../a
cd ..

ls <> abc

cat Makefile | grep pr | head -n 5 | hello
echo $?

pwd lalala

cd -

pwd a b c d

unset a1=10

export TMPPATH=$PATH
unset PATH
echo $PATH
export PATH=$TMPPATH
unset TMPPATH
echo $PATH

>file

ifcat < f2| wc -

export | env -> error ?

export

env

echo $CDPATH
export TMPCD=$CDPATH
export CDPATH=/
cd home/..
export CDPATH=$TMPCD
unset TMPCD
echo $CDPATH

ls><abc

export $var

cd ..
./duckinclude/mini_quack_shell.h

export TMPPATH=$PATH
unset PATH
ls
export PATH=$TMPPATH
unset TMPPATH
echo $PATH

echo "$    a"

> test | echo blabla

echo $SHLVL

echo "\\$HOME \\\$HOME \\\\$HOME $HOME $HOME $HOME"

  		ls   	 	   		     -l 		 	    -all	   

env -i ./minishell

                     

./efwufkk

unset a c=10

